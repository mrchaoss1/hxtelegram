package telegram.types;

/**
 * Represents thescopeof bot commands, covering all group and supergroup chats.
 */
typedef BotCommandScopeAllGroupChats = {
  /** Scope type, must be all_group_chats */
  type:String,
}
