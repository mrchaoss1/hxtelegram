package telegram.types;

/**
 * This object represents an inline keyboard button that copies specified text to the clipboard.
 */
typedef CopyTextButton = {
  /** The text to be copied to the clipboard; 1-256 characters */
  text:String,
}
